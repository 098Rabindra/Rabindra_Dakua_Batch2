{
  "name": "password-vault-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "backend:dev": "mvn spring-boot:run",
    "backend": "java -Dspring.profiles.active=h2 -jar target/password-vault-backend-1.0.0-SNAPSHOT.jar"
  }
}
